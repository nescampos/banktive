@model Banktive.Web.Models.WalletModel.ViewWalletViewModel
@{
    ViewData["Title"] = "View wallet "+Model.Wallet.Name;
}

<div class="row">
    <div class="col-3">
        <br/>
    </div>
    <div class="col-6">
        <h3>@ViewData["Title"]</h3>
    </div>
    <div class="col-3">
        <br/>
    </div>
</div>
<br />
<div class="row">
    <div class="col-12">
        @Html.ActionLink("Back", "Index","Wallet", null, new { @class="btn btn-info" })
    </div>
</div>
<br />


<div class="row">
    <div class="col-md-5">
        <h3>Account</h3>
        Name: @Model.Wallet.Name <br />
        Description: @Model.Wallet.Description <br />
        Alias: @Model.Wallet.Alias <br />
        Account Id: @Model.Wallet.Id <br />
        Currency: @Model.Wallet.Currency.Name (@Model.Wallet.Currency.Code) <br />
        Created at: @Model.Wallet.CreatedAt <br />
        QR: <div id="qrcode_address"></div> <br />
    </div>
    <div class="col-md-7">

        @*<h3>@Localizer["Últimos pagos"]</h3>

        <table class="table">
            <thead>
                <tr>
                    <th>@Localizer["Destinatario"]</th>
                    <th>@Localizer["Fecha"]</th>
                    <th>@Localizer["Monto"]</th>
                    <th>@Localizer["Comentario"]</th>
                </tr>
            </thead>

            @foreach (var transaction in Model.LastFivePayments)
            {
                <tr>
                    <td>
                        @transaction.DestinationAddress
                    </td>
                    <td>
                        @transaction.CreatedAt.ToLocalTime()
                    </td>
                    <td>
                        @transaction.Amount
                    </td>
                    <td>
                        @transaction.Comments
                    </td>
                </tr>
            }

        </table>*@
    </div>
</div>


@section Scripts
{
    @*<script type="text/javascript" src="/js/jquery.qrcode.min.js"></script>
    <script type="text/javascript">
        $(function () {
            $('#qrcode_address').qrcode('@Model.Wallet.Address');
        })

    </script>*@
}

